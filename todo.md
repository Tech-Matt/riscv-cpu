# Ideas

- for every stage create a testbench to test it;
- With a BRAM (block ram) if we ask for a response, we will receive it after 1 clock cycle, in this case we need to remove the instruction register, because the memory itself is a register, instead with a DRAM (Distributed memory, created by lookup tables) we will receive the answer in the same clock cycle.

# Tasks
